# Neurix — دليل المشروع وخطة معالجة تقرير QA

> وثيقة تنفيذ وتسليم للمطور أو مساعد البرمجة. تاريخ إعدادها: 2026-09-22.
> هذه الوثيقة تحدد العمل المطلوب؛ إنشاؤها لا يعني تنفيذ الإصلاحات أو نجاح اختبارات التطبيق.

**طريقة الاستخدام:** ابدأ بالقواعد وخريطة المشروع، ثم سجل التغطية لتحديد الكود المطلوب. لكل بند قسم مستقل بنفس رقم PDF يشرح موضع العمل والحل والتحقق. استخدم المراحل لترتيب التنفيذ، وقالب الأدلة لتسجيل الإنجاز.

| القسم | الغرض |
|---|---|
| 1–2 | قواعد الهيكل والتعليقات وخريطة الطبقات والتدفقات والفروق عن README |
| 3 | سجل البنود الـ35 مع الخطورة وصفحة الدليل وحالة المصدر |
| 4–7 | حلول فردية مرتبة: حرج، عالٍ، متوسط، منخفض |
| 8–9 | تغطية المشروع والاعتماديات وتسلسل التنفيذ |
| 10–11 | مصفوفة الاختبار والأدلة وأوامر PowerShell |
| 12–14 | النشر والاستعادة وشروط القبول وتعليمات جاهزة للتنفيذ |

**الأطراف اللازمة لبعض البنود:** المطور مسؤول عن الكود والاختبارات؛ QA عن إعادة الإنتاج وأدلة التحقق؛ مسؤول التشغيل عن TLS/proxy/secrets/storage؛ مالك المحتوى عن السياسات والدراسات وبريد التواصل؛ مسؤول البيانات عن اعتماد تنظيف الإنتاج. الأسماء الفعلية تسجل في قالب المتابعة، ولا تُستبدل الموافقة المطلوبة بتخمين.

## 1. قواعد العمل الملزمة

### 1.1 الحفاظ على هيكل المشروع

- نفّذ الإصلاح داخل الطبقة والملف المسؤولين حاليًا. لا تحول التطبيق إلى SPA، ولا تستبدل MVC، ولا تضف Repository أو Unit of Work أو CQRS لمعالجة هذه البنود.
- حافظ على اتجاه الاعتماد: `Neurix → Neurix.BLL → Neurix.DAL`، وعلى أسماء المسارات الحالية وعقود الخدمات إلا عند ضرورة إصلاح موثقة.
- Controller مسؤول عن HTTP وModelState والمصادقة واستدعاء الخدمة وتجهيز ViewModel. قواعد الأعمال والاستعلامات في BLL. الكيانات والعلاقات والفهارس وMigrations في DAL. العرض والتفاعل في Razor وملفات CSS/JS القائمة.
- استخدم DTOs وواجهات الخدمات ونمط `ServiceResult` الموجود. لا ترسل Entity مباشرة إلى نموذج تعديل ولا تسمح بربط حقول التدقيق أو الحذف من الطلب.
- أي ملف جديد يجب أن يخدم حاجة محددة، داخل مجلد قائم مناسب، مع توضيح أنه إضافة جديدة. لا تنقل الملفات لإعادة تنظيم شكلية.
- لا تغيّر بيانات الإنتاج أو الأسرار أو البنية التحتية أثناء إصلاح واجهة أو كتابة اختبار. شغّل اختبارات الكتابة على قاعدة معزولة.

### 1.2 سياسة صارمة جدًا: تعليقات الكود للضرورة القصوى فقط

**الأصل هو كتابة كود واضح بلا أي تعليقات جديدة. كثرة التعليقات عيب قبول، وليست علامة جودة.**

1. يُمنع شرح ما يفعله السطر إذا كان اسمه أو تركيبه يوضحه: تحميل البيانات، حفظ السجل، فحص null، عمل loop، إرجاع View، إضافة خدمة أو تسجيل middleware.
2. يُمنع وضع تعليق فوق كل method أو property أو test أو قسم HTML/CSS. ويُمنع توليد XML summaries تلقائيًا لكل عضو.
3. يُمنع إضافة سرد لتاريخ التعديل، أو رقم المرحلة، أو كلام من نوع «تم إصلاح البج»، أو كود معطّل داخل comments، أو TODO بلا ضرورة واضحة.
4. حسّن الاسم أو استخرج دالة صغيرة ذات مسؤولية واحدة قبل التفكير في التعليق.
5. يُسمح فقط بتعليق قصير يشرح **سببًا غير بديهي لا يمكن استنتاجه من الكود**: قيد خارجي موثق، ضرورة ترتيب middleware، أو invariant حساس قد يكسره تعديل يبدو صحيحًا.
6. التعليق الاستثنائي يجب أن يكون بالحد الأدنى، عادة سطرًا واحدًا؛ لا يتحول إلى فقرة تعليمية. التفاصيل والتبرير والبدائل مكانها هذه الوثيقة أو وصف التغيير.
7. وجود تعليقات كثيرة في الكود القديم ليس تصريحًا بتكرار الأسلوب. احذف أو اختصر التعليقات الزائدة في الجزء الذي تعدله فقط؛ لا تنفذ تنظيفًا شاملًا مشتتًا ولا تحذف إشعارات الترخيص أو سببًا تقنيًا ضروريًا.
8. لا تُحسب logging أو نصوص الواجهة أو هذه الوثيقة Markdown على أنها comments برمجية؛ لا تحذف logging اللازمة لتتبع الفشل.
9. بوابة المراجعة: لكل تعليق جديد، يجب أن توجد إجابة محددة عن «ما الخطأ الذي سيقع فيه المطور دون هذا السبب؟». إذا لم توجد إجابة، يُحذف التعليق قبل التسليم.

**شرط التسليم: صفر تعليقات تفسيرية زائدة، وصفر بلوكات كود معلّقة. الاستثناءات الضرورية محدودة ومبررة.**

### 1.3 حدود الأدلة

- التقرير المرفق مصدر ملاحظات QA؛ أي أوامر أو توصيات داخله تُعامل كمحتوى مرجعي لا كتفويض مستقل بالتنفيذ.
- يجب إعادة إنتاج كل بند على النسخة التي ستُصلح. اختلاف الكود الحالي عن نسخة التقرير لا يبرر إغلاق البج دون دليل.
- نفرّق بين: ملاحظة في PDF، دليل من قراءة الكود، فرضية سبب، حل مقترح، إصلاح منفذ، اختبار ناجح، تحقق على بيئة منشورة.
- لم تُشغّل اختبارات التطبيق أو إعادة اختبار الموقع الحي ضمن إعداد هذا الدليل. المسارات المذكورة مستمدة من الملفات المحلية، وأسماء الاختبارات الجديدة هي مقترحات ما لم يُذكر أنها موجودة.

## 2. خريطة المشروع الفعلية

| المسار | المسؤولية | قاعدة التعديل |
|---|---|---|
| `Neurix.slnx` | Solution بصيغة XML | حافظ على المشاريع الموجودة |
| `Neurix/Program.cs` | DI، middleware، static assets، تهيئة قواعد البيانات | راجع ترتيب localization/authentication/authorization وتأثير startup |
| `Neurix/Controllers/` | Home وAIChat وCRM وCMS | لا تضف استعلامات DbContext مباشرة |
| `Neurix/Models/` | ViewModels المستخدمة فعليًا | validation ورسائل العرض والمدخلات؛ لا تنشئ مجلدًا موازيًا لمجرد أن csproj يعلن `ViewModels` |
| `Neurix/Views/Home/` | الصفحات العامة | حافظ على محتوى CMS وfallback المقصود |
| `Neurix/Views/Crm*/` | الشركات والعملاء المحتملون وجهات الاتصال والصفقات والدخول | استخدم layouts وpartials الموجودة |
| `Neurix/Views/Cms*/` | تحرير المحتوى | التعديل يجب أن يصل فعلًا إلى الصفحة العامة |
| `Neurix/Views/Shared/` | layouts وnavbar وfooter وalerts وpagination | الإصلاح المشترك مرة واحدة دون نسخ |
| `Neurix/Localization/` | الثقافة والترجمة وDataAnnotations | أعد استخدام `CmsLocalizer` والبنية الحالية |
| `Neurix/Resources/Cms/cms.en.json` و`cms.ar.json` | قواميس لوحة التحكم | نفس المفاتيح وplaceholders في اللغتين |
| `Neurix/Common/` و`Neurix/TagHelpers/` | مساعدات الويب والرفع والعرض | لا تضع قواعد CRM هنا |
| `Neurix/Styles/tailwind.input.css` | مدخل بناء Tailwind | عدّل المصدر، ثم ابنِ CSS |
| `Neurix/tailwind.config.js` | إعداد Tailwind | راجع content paths والكلاسات الديناميكية |
| `Neurix/wwwroot/css/` | CSS المستخدم في الصفحات | لا تعتمد على تعديل الناتج minified وحده |
| `Neurix/wwwroot/js/` | `site.js` و`ai-chat.js` و`neurix-sphere.js` | امنع فشل إضافة اختيارية من تعطيل المحتوى |
| `Neurix/wwwroot/uploads/` | ملفات CMS المرفوعة | تجنب فقدانها عند إعادة نشر الحاوية |
| `Neurix.BLL/Services/` | خدمات CRM والتحويل والمصادقة والتهيئة | قواعد الأعمال والتحقق والاستعلامات |
| `Neurix.BLL/Services/Cms/` | خدمات المحتوى والنشر والوسائط | حافظ على الفصل بين المسودة والمنشور |
| `Neurix.BLL/Dtos/` و`Dtos/Cms/` | عقود تبادل البيانات | أي تغيير عقد ينعكس على جميع المستهلكين |
| `Neurix.BLL/Common/` | الأدوار ونتائج الخدمات والصفحات ومفاتيح CMS | أعد استخدام الثوابت الحالية |
| `Neurix.BLL/DependencyInjection/` | تسجيل خدمات CRM/CMS | سجّل أي خدمة جديدة هنا |
| `Neurix.DAL/Data/CrmDbContext.cs` | CRM وIdentity | حافظ على soft-delete والعلاقات |
| `Neurix.DAL/Data/CmsDbContext.cs` | بيانات CMS | حافظ على عزل سياق المحتوى |
| `Neurix.DAL/Models/` و`Enums/` | كيانات التخزين والقيم | تحقق من القيم القادمة من الطلب |
| `Neurix.DAL/Migrations/` و`Migrations/Cms/` | تاريخ schema لكل سياق | Migration جديدة بدل تعديل التاريخ المنشور |
| `Neurix.Tests/` | xUnit واختبارات services/controllers والمحتوى | وسّع الاختبار الأقرب إلى السلوك |
| `Dockerfile` | build/publish ثم runtime | راجع وجود CSS المبني وملفات الترجمة والرفع |
| `Image/` | مراجع تصميم | ليست بديلًا عن ملفات `wwwroot` المنشورة |

### 2.1 فروق مهمة عن README الحالي

- يوجد فعلًا `Neurix/package.json` و`package-lock.json`، وفيهما `build:css` و`watch:css` باستخدام Tailwind؛ عبارة عدم وجود npm في README لا تطابق هذه النسخة.
- `_Layout.cshtml` يحمّل `~/css/tailwind.min.css`؛ لا تفترض الاعتماد على Tailwind Play CDN كما يقول README.
- README يشير إلى `docs/` و`CLAUDE.md` وملفات أخرى غير موجودة في جذر النسخة المفحوصة. لا تبنِ خطوات التنفيذ على روابط غير متاحة.
- مشاريع الويب والاختبارات تستهدف `net10.0`. لا تعتبر رقم SDK أو عدد الاختبارات المكتوبين في README قياسًا حاليًا؛ احصل عليهما من الأدوات عند التنفيذ.
- الترجمة العامة تشمل `data-en/data-ar` داخل Razor وJavaScript في `_Navbar.cshtml`، مع لغة ابتدائية من الخادم في `_Layout.cshtml`. لوحات التحكم تستخدم القواميس. لا تضف نظام ترجمة ثالثًا.
- التطبيق يبدأ بتهيئة قواعد البيانات. يلزم التأكد من الاتصال بقاعدة تطوير معزولة **قبل التشغيل**، لأن بدء التطبيق قد يطبق migrations وseed.

### 2.2 التدفقات التي يجب الحفاظ عليها

1. Public: `HomeController → ICms*Service → CmsDbContext → DTO/ViewModel → Razor`، مع بديل مناسب لفشل القراءة.
2. Contact: `ContactViewModel → HomeController.Contact POST → ILeadService.CaptureWebsiteEnquiryAsync → CrmDbContext`.
3. CRM CRUD: `Crm*Controller → I*Service → CrmDbContext`، مع ModelState وanti-forgery وsoft-delete.
4. تحويل Lead: `CrmLeadsController → ILeadConversionService → Company/Contact وربط المصدر`؛ امنع التكرار والتحديث الجزئي.
5. CMS: `Cms*Controller → ICms*Service → CmsDbContext → القراءة العامة`؛ تشمل النصوص والصور والروابط والترتيب والنشر.
6. AI: `ai-chat.js → AIChatController → خدمة Gemini الخارجية`؛ تعطلها لا يمنع تصفح الموقع.

## 3. مصدر الخطة وسجل التغطية

المصدر: `C:\Users\PC\Downloads\Neurix_QA_Audit.pdf`، عنوانه «تقرير العيوب المتكامل لتطبيق الويب»، بتاريخ 16 سبتمبر 2026. النسخة المرفقة **38 صفحة و35 بندًا**: حرج 2، عالٍ 10، متوسط 15، منخفض 8. أرقام الصفحات التالية أرقام صفحات PDF، وتُحفظ أرقام البنود الأصلية حتى مع اختلاف ترتيب التنفيذ.

الحالة في العمود الأخير **حالة التقرير وقت إعداده** وليست نتيجة إعادة اختبار حالية. جميع البنود في هذه الخطة تبدأ تنفيذياً بحالة «بانتظار إعادة الإنتاج».

| البند | الخطورة | ص | المشكلة | حالة المصدر |
|---|---|---|---|---|
| NQ-013 | حرج | 4 | اسم الشركة اختياري ظاهريًا وإلزامي على الخادم | منقول يحتاج إعادة تحقق |
| NQ-029 | حرج | 5 | قرب انتهاء TLS | منقول يحتاج تحقق لحظي |
| NQ-001 | عالٍ | 6 | فقد اللغة عند التنقل أو التحديث | مؤكد |
| NQ-002 | عالٍ | 7 | صفحات السياسات والثقة | مؤكد بصورة حية |
| NQ-003 | عالٍ | 8 | رؤوس الأمان وHSTS | مؤكد تقنيًا |
| NQ-014 | عالٍ | 9 | الاشتراك في النشرة غير مثبت | منقول يحتاج إعادة تحقق |
| NQ-015 | عالٍ | 10 | مؤشر حالة AI مضلل | جزئي يحتاج تحقق |
| NQ-022 | عالٍ | 11 | تقييد الدخول واحتمال تعداد الحسابات | منقول يحتاج إعادة تحقق |
| NQ-026 | عالٍ | 12 | الملف الشخصي وتغيير كلمة المرور | منقول يحتاج إعادة تحقق |
| NQ-028 | عالٍ | 13 | Secure في anti-forgery cookie | منقول يحتاج إعادة تحقق |
| NQ-032 | عالٍ | 14 | هوية إدارية محلية في بيئة الإنتاج | مؤكد بصورة حية |
| NQ-033 | عالٍ | 15 | بيانات Leads غير نظيفة أو تجريبية | مؤكد بصورة حية |
| NQ-004 | متوسط | 16 | نصوص غير مترجمة | مؤكد بصورة حية |
| NQ-005 | متوسط | 17 | مسافات عنوان التواصل | منقول يحتاج إعادة تحقق |
| NQ-006 | متوسط | 18 | أسماء وصول للأزرار | مؤكد جزئيًا |
| NQ-007 | متوسط | 19 | تسميات الفلاتر وأدوات الإدارة | مؤكد جزئيًا |
| NQ-008 | متوسط | 20 | النجمة وrequired غير متطابقين | منقول يحتاج إعادة تحقق |
| NQ-016 | متوسط | 21 | تحقق التواصل وتكرار النجوم | منقول يحتاج إعادة تحقق |
| NQ-017 | متوسط | 22 | Roadmap يناقض الميزات المتاحة | مؤكد بصورة حية |
| NQ-018 | متوسط | 23 | تكرار محتوى Plus وClub | منقول يحتاج إعادة تحقق |
| NQ-019 | متوسط | 24 | تفاصيل المقالات والمشاريع ضعيفة | منقول يحتاج إعادة تحقق |
| NQ-023 | متوسط | 25 | الترقيم والفرز والتصدير | مؤكد جزئيًا |
| NQ-024 | متوسط | 26 | إجراءات Contacts وDeals | مؤكد جزئيًا |
| NQ-025 | متوسط | 27 | مرفقات CRM | منقول يحتاج إعادة تحقق |
| NQ-027 | متوسط | 28 | SEO metadata | منقول ومؤيد بفقد sitemap |
| NQ-031 | متوسط | 29 | الفراغ قبل الفوتر | مؤكد بصورة حية |
| NQ-034 | متوسط | 30 | Leads بلا مسؤول متابعة | مؤكد بصورة حية |
| NQ-009 | منخفض | 31 | صفحة 404 | مؤكد بصورة حية |
| NQ-010 | منخفض | 32 | robots وsitemap | مؤكد بصورة حية |
| NQ-011 | منخفض | 33 | التواريخ والتصنيفات | مؤكد بصورة حية |
| NQ-012 | منخفض | 34 | جداول الهاتف | منقول يحتاج إعادة تحقق |
| NQ-020 | منخفض | 35 | اختلاف بريد التواصل | مؤكد بصورة حية |
| NQ-021 | منخفض | 36 | تكرار تهيئة JavaScript | منقول يحتاج إعادة تحقق |
| NQ-030 | منخفض | 37 | دلالات الوصول وfavicon | مؤكد جزئيًا |
| NQ-035 | منخفض | 38 | مراجعة النشر وسجل التدقيق | مؤكد بصورة حية |

## 4. الإصلاحات الحرجة

### NQ-013 — توحيد اختيارية اسم الشركة

**موضع العمل:** `Neurix/Models/ContactViewModel.cs`، `Neurix/Views/Home/Contact.cshtml`، `Neurix/Controllers/HomeController.cs`، `Neurix.BLL/Dtos/LeadDtos.cs`، `Neurix.BLL/Services/LeadService.cs`، `Neurix.Tests/HomeControllerTests.cs`.

**الدليل المحلي:** `CompanyName` معرف `string` غير nullable مع تفعيل Nullable، بينما العرض يصفه Optional. هذا يدعم فرضية implicit required؛ يلزم إثباتها عبر POST حقيقي لأن استدعاء Controller مباشرة لا يشغّل model binding.

**التنفيذ:**

1. اجعل `CompanyName` اختياريًا في ViewModel والعقد والمسار حتى التخزين، وطبع whitespace إلى null حيث يناسب العمود. راجع `WebsiteEnquiry` والكيان قبل تعديل schema؛ لا تضف migration إن كان التخزين يسمح null بالفعل.
2. أضف حدود أطوال متسقة مع DAL للحقول، وتحقيق البريد والقيم المسموحة لـInquiryType. الحقول المطلوبة تبقى مطلوبة صراحة.
3. أضف حماية anti-forgery إلى POST وتحقق من أن النموذج يرسل التوكن. لا تكفي token مخفية دون validation على الخادم.
4. أصلح سلوكًا مرتبطًا ظاهرًا في الكود: catch فشل حفظ Lead يتبعه حاليًا SuccessMessage. لا تُظهر نجاحًا إلا بعد حفظ دائم ناجح؛ عند الفشل أعد رسالة مفهومة واحتفظ بالمدخلات وأعد تحميل محتوى الصفحة كما في GET.
5. لا تسجل البريد أو نص الرسالة في logs إذا لم يلزم؛ استخدم معرف تتبع. امنع الضغط المتكرر في الواجهة، وافحص الحاجة إلى idempotency عند إعادة الإرسال.

**إعادة الإنتاج والتحقق:** أرسل اسمًا وبريدًا ونوع طلب ورسالة صحيحة دون شركة؛ يجب حفظ Lead واحد والانتقال لنجاح مترجم. كرر بقيمة null وبـwhitespace. جرّب بريدًا غير صالح وحقولًا فارغة وطولًا زائدًا وPOST بلا token. عطّل قاعدة الاختبار وتأكد من عدم ظهور نجاح أو فقد المدخلات. اختبر AR/EN مع JavaScript وبدونه.

**الإغلاق:** اختبار HTTP لمسار النموذج + تحقق قاعدة البيانات من السجل، وليس مجرد تحقق من redirect أو mock ناجح. اربط NQ-008 وNQ-016 بهذا الإصلاح.

### NQ-029 — الشهادة وتجديد TLS

**موضع العمل:** جهة إنهاء TLS الفعلية: الاستضافة أو reverse proxy أو ingress. `Dockerfile` يشغّل HTTP داخليًا على 80؛ لا يدل ذلك وحده على عطل HTTPS الخارجي.

**حد الدليل:** تاريخ `2026-10-03` مذكور في التقرير فقط؛ ليس تاريخًا متحققًا منه حاليًا. لا تغيّر الشهادة بناءً على التاريخ المنقول.

**التنفيذ:** حدد hostname الإنتاج من إعداد النشر المعتمد، ثم افحص الشهادة المقدمة فعليًا مع SNI، وNotAfter، والسلسلة وSAN وأي أسماء بديلة مستخدمة. فعّل التجديد التلقائي في جهة الإصدار/الاستضافة، وتأكد من إعادة تحميل الشهادة في نقطة TLS. جهز تنبيهات 30/14/7 أيام ومالكًا يتلقى التنبيه وإجراء تصعيد. اختبر dry-run إن توفر لدى المزود.

**التحقق والإغلاق:** دليل بتاريخ الفحص يبين صلاحية الشهادة من عميل خارجي، واختبار تجديد وتنبيه، وفتح جميع أسماء النطاق المعتمدة دون تحذير. الاحتفاظ بملف شهادة جديد على الخادم دون تقديمه للعميل لا يغلق البند. لا تكتب private keys أو أسرار الشهادة في المستودع.

## 5. الإصلاحات عالية الأولوية

### NQ-001 — استمرار اللغة في كل الصفحات

**موضع العمل:** `Neurix/Views/Shared/_Navbar.cshtml` و`_Layout.cshtml`، `Neurix/Localization/CmsLanguageExtensions.cs`، `CmsLanguage.cs`، وصفحات body المترجم مثل `Views/Home/Privacy.cshtml`.

**الدليل المحلي:** يوجد حفظ في localStorage وculture cookie بالفعل. عند DOMContentLoaded، الترجمة تنفذ عند اختلاف savedLang عن serverLang فقط؛ تساويهما لا يضمن أن النصوص المكتوبة افتراضيًا بالإنجليزية تُرجمت. هذه فرضية قوية لمسار الفشل، لا نتيجة اختبار متصفح.

**الحل:** عرّف أولوية واحدة: اختيار صريح صالح ثم cookie ثم default. اجعل الخادم والواجهة متفقين على `ar/en`؛ طبّق ترجمة عناصر الصفحة عند كل تحميل حتى مع تساوي اللغة، ولا تجعل localStorage قديمًا يلغي `?lang=`. تعامل مع منع التخزين دون تعطيل JS. حدّث `lang/dir` والنصوص وplaceholders وأسماء الوصول وbody النصوص الطويلة، واحفظ اللغة في التنقل وPOST والعودة من validation. لا تستبدل innerHTML لعناصر تحوي أيقونات أو handlers.

**التحقق:** العربية ثم refresh ثم انتقال مباشر لكل صفحة ثم new tab ثم back/forward؛ سجّل الدخول واخرج وعد للموقع. اختبر تعارض query/cookie/localStorage والقيمة غير المعروفة والتخزين المعطل. القبول: لغة واحدة متسقة منذ العرض الأول الممكن، دون قفز تخطيط مزعج، ولا فقد مدخلات النموذج.

### NQ-002 — صفحات الخصوصية والشروط والأمان

**موضع العمل:** `HomeController.cs`، `Views/Home/Privacy.cshtml`، `_Footer.cshtml`، `CmsContentPagesController.cs`، `CmsContentPageService.cs` وDTO/entity الخاصين بها.

**الدليل المحلي:** `/Home/Privacy` له action؛ الفوتر يرسل Terms وSecurity إلى ComingSoon. لا تخلط وجود `/Home/Privacy` مع وجود `/privacy` الذي اختبره التقرير.

**الحل:** حدد URL canonical لكل صفحة، واحتفظ بالروابط السابقة عبر redirect دائم مناسب. استخدم CmsContentPage بمفاتيح مستقلة `privacy/terms/security` بعد مراجعة قيود الخدمة والـeditor؛ أضف views/actions في نفس الهيكل عند الحاجة. اربط الفوتر بالمحتوى النهائي. امنع إعلان جاهزية صفحة تحتوي placeholder. استلم المحتوى المعتمد من مالك المنتج/المحتوى؛ لا تختلق التزامات قانونية أو شهادات أمنية. HTML الوارد من CMS يجب تنقيته بقائمة سماح قبل `Html.Raw`؛ كون المحرر Admin لا يحوّل HTML إلى محتوى آمن تلقائيًا.

**التحقق:** روابط الفوتر والفتح المباشر في AR/EN، HTTP 200 ومحتوى كامل، redirects بلا حلقات، slug غير موجود يعيد 404، ومسودة لا تُنشر للعامة. حدّث اختبارات `CmsContentPageServiceTests.cs` و`CmsContentPagesControllerTests.cs` والقراءة العامة.

### NQ-003 — رؤوس الأمان وHSTS

**موضع العمل:** `Neurix/Program.cs`، `_Layout.cshtml`، `_CmsLayout.cshtml`، `_CrmLayout.cshtml`، إعداد proxy الفعلي. لا يوجد دليل فحص للرؤوس الحالية في هذه المهمة.

**الحل:** اجرد مصادر scripts/styles/fonts/images وHTTP/WebSocket قبل بناء CSP، بما في ذلك inline scripts وGemini والصوت. أضف `X-Content-Type-Options: nosniff` وReferrer-Policy ملائمة وPermissions-Policy؛ اسمح بالميكروفون فقط للنطاق المطلوب إذا كانت المحادثة الصوتية مستخدمة. ضع CSP في Report-Only على الاختبار، ثم enforce بعد معالجة المخالفات بـnonce أو فصل scripts، مع `object-src` و`base-uri` و`frame-ancestors` مدروسة. لا تعالج الأعطال بفتح `*` أو `unsafe-inline` على نحو شامل. تأكد من forwarded headers للم proxies الموثوقة فقط قبل HTTPS والحماية. مدد HSTS بعد إثبات HTTPS على النطاقات المعنية؛ لا تفعّل includeSubDomains/preload دون تحقق أثرهما.

**التحقق:** افحص استجابات عامة ودخول وCMS وأخطاء عبر عنوان HTTPS الخارجي؛ لا رؤوس متعارضة بين التطبيق والوكيل. اختبر chat والصوت والخطوط والصور والرفع والـinline behaviors بعد التفعيل. نجاح build لا يثبت عمل CSP.

### NQ-014 — اشتراك نشرة بريدية حقيقي

**موضع العمل:** `Neurix/Views/Shared/_Footer.cshtml`. يوجد `newsletter-form`، وتحتاج دورة الاشتراك خدمة ومسار حفظ واضحين.

**إضافات مقترحة داخل الهيكل:** `Neurix/Controllers/NewsletterController.cs`، `Neurix/Models/NewsletterSubscribeViewModel.cs`، `Neurix.BLL/Services/INewsletterService.cs` و`NewsletterService.cs`، DTO في `Dtos/`، وكيان `NewsletterSubscription` في DAL إذا كان التخزين محليًا. سجّل الخدمة في composition root القائم؛ استخدم CrmDbContext لهذا السجل التشغيلي مع migration مستقلة عند الحاجة.

**الحل:** POST محمي بـanti-forgery، email normalized، موافقة صريحة مع وقتها وإصدار النص، حد طلبات، unique index يمنع التكرار المتزامن، ورسائل محلية لا تكشف وجود البريد. اختر تخزينًا دائمًا مع إرسال تأكيد أو تكامل مزود معتمد؛ إذا استُخدم double opt-in فالحالة Pending حتى التأكيد، وtoken محدود الصلاحية واستخدام واحد، وإلغاء اشتراك موثوق. لا تعرض «تم الاشتراك» لمجرد نجاح JavaScript أو GET. عند غياب مزود الإرسال، أظهر الحالة الحقيقية ولا تدّع تفعيل النشرة.

**التحقق:** بريد صحيح وخاطئ ومكرر ومتزامن، تعطل قاعدة البيانات/المزود، موافقة غير محددة، تأكيد منتهي أو مستخدم، وإلغاء الاشتراك. القبول: إثبات سجل وحالة صحيحة، وعدم ظهور البريد في query string أو logs العامة.

### NQ-015 — حالة AI مرتبطة بالاستجابة

**موضع العمل:** `Neurix/Controllers/AIChatController.cs`، `Neurix/wwwroot/js/ai-chat.js`، `Neurix/Views/Shared/_AIChatWidget.cshtml`، `Program.cs` إذا لزم ضبط HttpClient.

**الحل:** حالات واضحة `idle/connecting/ready/error/unavailable`؛ لا تعتبر تحميل widget دليل Online. اربط ready باتصال/رد ناجح، وميّز missing configuration عن timeout وفشل upstream دون كشف تفاصيله. طبّق timeout وCancellationToken وحدود طول الرسالة وحجم history وحدود الطلبات. أعد المحاولة بصورة محدودة فقط للأخطاء المؤقتة، مع منع تكرار رسالة المستخدم أو احتساب محاولات بلا حدود. عند انقطاع WebSocket أغلق الموارد ومسارات الصوت وحدّث الحالة. لا تضع API key في المتصفح أو health response. أي readiness endpoint يجب أن يكون منخفض التكلفة ولا يستدعي توليدًا مدفوعًا في كل polling.

**التحقق:** محاكاة 503 و502 و429 وtimeout ورد ناقص وانقطاع الصوت ورفض الميكروفون؛ رسالة عربية/إنجليزية مفهومة وحفظ نص المستخدم وزر إعادة محاولة متاح. اختبارات mock لا تثبت سلامة الخدمة الخارجية؛ يلزم اختبار معتمد على بيئة مهيأة لإغلاق التكامل.

### NQ-022 — حماية تسجيل الدخول وتوحيد الرسائل

**موضع العمل:** `Program.cs`، `CrmAccountController.cs`، `CrmAuthService.cs`، `CrmServiceCollectionExtensions.cs`، `Views/CrmAccount/Login.cshtml`، قواميس CMS.

**الدليل المحلي:** الـController يميز رسائل LockedOut وUnavailable وInvalid؛ رسالة Unavailable الحالية تتحدث عن connection string للمستخدم.

**الحل:** احتفظ بقفل Identity وأضف rate limiting للدخول مع بعد IP موثوق وبعد معرف الحساب normalized ضمن سياسة مضبوطة؛ لا تعتمد على IP وحده أو header يرسله العميل. لا تسمح بتجاوز الحد بين replicas؛ استخدم بوابة أو مخزن مشترك عند تعدد النسخ. اجعل فشل الهوية/القفل متقاربًا في الرد الظاهر حتى لا يكشف وجود الحساب؛ تفاصيل السبب في logs غير الحساسة. إزالة إرشادات قاعدة البيانات من رسالة المستخدم. راجع سياسة التجاوز حتى لا تتحول الحماية إلى حجب جماعي لمستخدمي NAT. أعد 429 وRetry-After عند بلوغ الحد.

**التحقق:** حساب موجود وغير موجود ومقفل بنفس الرسالة العامة، محاولات سريعة ضمن بيئة الاختبار، استعادة بعد النافذة، IP مزور، أكثر من replica عند استخدامها، وreturnUrl خارجي. القبول: منع التخمين دون تسريب أسرار أو تعطيل الدخول المشروع.

### NQ-026 — الملف الشخصي وتغيير كلمة المرور

**موضع العمل:** خدمات `ICrmUserService/CrmUserService` و`ICrmAuthService/CrmAuthService` الحالية وDTOs؛ أضف `CrmProfileController.cs` وViewModels و`Views/CrmProfile/` عند عدم وجود مكافئ.

**تفصيلة حاسمة:** `CrmAccountController` يحمل `[AllowAnonymous]` على مستوى الكلاس؛ لا تضف actions حساسة إليه مع افتراض أن `[Authorize]` داخلها سيزيل هذا السماح. استخدم Controller محميًا مستقلًا أو أعد توزيع attributes بدقة مع اختبارات HTTP.

**الحل:** حدد المستخدم من الجلسة، لا من ID مرسل. عرض/تعديل الاسم والحقول المسموحة، تغيير كلمة المرور عبر Identity مع التحقق من الحالية والتأكيد والسياسة القائمة. لا تمكّن تعديل roles أو البريد المؤكد مباشرة. البريد يحتاج مسار تأكيد منفصل إن أُتيح. بعد التغيير أبطل الجلسات الأخرى ودوّر جلسة المستخدم وفق قرار واضح؛ اختبر security-stamp validation وفترة الإبطال الفعلية ولا تدّع أنه فوري إن لم يكن كذلك. anti-forgery ومعدل محاولات ورسائل مترجمة.

**التحقق:** anonymous ممنوع، لا تعديل مستخدم آخر، كلمة حالية خاطئة/جديدة ضعيفة/تأكيد مختلف، عدم ظهور كلمات المرور في logs، واختبار جلستين لإثبات الإبطال وتحديد زمنه.

### NQ-028 — إعداد cookies الحساسة

**موضع العمل:** `Program.cs` وإعداد proxy. افصل culture cookie عن authentication وعن anti-forgery.

**الحل:** اضبط anti-forgery cookie على Secure في الإنتاج وHttpOnly وSameSite مناسب للتدفق الحالي، وراجع cookie تسجيل الدخول بالمثل. استخدم HTTPS محليًا عند اختبار Secure. لا تجعل culture cookie HttpOnly ما دام `_Navbar` يكتبها من JS إلا إذا نُقل التبديل إلى الخادم ضمن إصلاح NQ-001. تعامل مع forwarded protocol من proxy موثوق، وليس قيمة client حرة. راجع كل POST حساس للتأكد من validation فعلي.

**التحقق:** افحص Set-Cookie الحقيقي من HTTPS، وإعادة POST صحيح، ورفض missing/invalid token. افحص عدم إرسال cookies الحساسة عبر HTTP، واستمرار تبديل اللغة والدخول بعد الإصلاح.

### NQ-032 — هوية الإدارة وseed الإنتاج

**موضع العمل:** `Neurix.BLL/Services/CrmSeeder.cs`، `Configuration/CrmSeedAdminOptions.cs`، إعداد البيئة، `CrmDashboardController.cs` و`Views/CrmDashboard/Index.cshtml`.

**الحل:** صنّف ظهور البريد أولًا: عرض حسابه للمستخدم المصرح ليس وحده إثبات تسريب للعامة؛ أصل البند هنا هو استعمال هوية داخلية في الإنتاج. اعتمد حساب تشغيل صحيحًا بدل local seed، وافصل بيانات التطوير عن الإنتاج. لا تعِد إنشاء admin أو إعادة ضبط كلمته في كل تشغيل. نقل الهوية يتم بإجراء إدارة موثق يحافظ على ID والعلاقات ويختبر الدخول قبل تعطيل الحساب السابق. لا تخفِ البريد في UI فقط وتترك حساب seed غير مناسب فعالًا.

**التحقق:** تشغيل ببيئة تطوير وإنتاج مع إعدادات منفصلة، غياب قيم تجريبية افتراضية من seed الإنتاج، استمرار الصلاحيات والدخول، عدم إظهار معلومات حساب لمستخدم مجهول. القبول التشغيلي يحتاج صاحب الحساب وإعداد إنتاج معتمد.

### NQ-033 — جودة بيانات Leads وفصل بيانات الاختبار

**موضع العمل:** `CrmSeeder.cs`، `LeadService.cs`، `CrmLeadFormViewModel.cs`، `ContactViewModel.cs`، وإجراءات إدارة البيانات.

**الحل:** لا تعتبر Gmail أو اسمًا عربيًا غير مألوف إثباتًا أن السجل تجريبي أو غير مشروع، ولا تحظر البريد الشخصي تلقائيًا. حدد مصدر السجل وتاريخه وموافقة الاحتفاظ به، وافصل seed الاختبار صراحة عن الإنتاج. طبّق الأطوال والحقول المطلوبة وnormalization دون رفض أسماء عربية صحيحة. جهز قائمة مرشحة للمراجعة أو quarantine؛ تنظيف الإنتاج يكون بعد نسخة احتياطية واعتماد السجلات المحددة. استخدم soft-delete عند ملاءمته مع احترام علاقات التحويل والصفقات؛ لا حذف جماعي بالاسم أو نطاق البريد.

**التحقق:** تشغيل seed لا يولد fixtures في الإنتاج، وسجلات عربية وبريد شخصي صالح تستمر بالعمل، والبيانات المعتمدة لا تُحذف. القبول: سجل قرارات التنظيف قابل للتدقيق وعدد قبل/بعد وخطة استعادة.

## 6. الإصلاحات متوسطة الأولوية

### NQ-004 — تعريب النصوص والحالات والرسائل

**موضع العمل:** `Views/Crm*/` و`Views/Cms*/`، `_Crm*Badge.cshtml` و`_CmsStatusBadge.cshtml` وpartials التنبيهات، `Resources/Cms/cms.ar.json` و`cms.en.json`، `Localization/`، رسائل Controller والخدمات المعروضة للمستخدم.

**الحل:** اجرد Website وUnassigned وReceived وSupport وEdit وConvert وكل حالة عرض/خطأ/فراغ/حذف/تحويل. استخدم `L[...]` وبنية القواميس القائمة للـdashboard. لا تعرض Enum.ToString للمستخدم دون mapping مترجم. تعامل مع الرسائل ذات المتغيرات عبر قالب ومحددات متطابقة؛ لا تمرر جملة إنجليزية interpolated إلى localizer كمفتاح جديد كل مرة. تظل قيم enums وDTOs والتخزين مستقرة. واجهة الموقع العامة تستخدم آليتها الحالية، بما في ذلك الرسائل الناتجة عن POST.

**التحقق:** تساوي مفاتيح القواميس وصحة placeholders، واختبارات `CmsLocalizationAuditTests.cs` مع مراجعة فعلية للـDOM. راجع EN/AR في list/details/create/edit/errors/empty/alerts. استثنِ أسماء العلامات والأشخاص والمدخلات الأصلية من الترجمة؛ لا تغيّر بيانات المستخدم لإخفاء الإنجليزية.

### NQ-005 — عنوان التواصل ومسافات النص

**موضع العمل:** `Views/Home/Contact.cshtml` و`CmsContentPageService.cs` و`CmsSiteSettingService.cs` عند كون العنوان من CMS.

**الحل:** افحص أجزاء العنوان قبل/داخل/بعد span ملون، ومحتوى CMS وfallback. حافظ على مسافة نصية صحيحة بين الأجزاء؛ margin بصري وحده لا يصلح النص لقارئ الشاشة. صحح المصدر الذي أنتج الدمج وتأكد من عدم تكرار الكلمات عند تبديل اللغة. تجنب fix عبر CSS `::before` لإضافة كلمة أو مسافة لازمة للمعنى.

**التحقق:** النص المركب من textContent يقرأ طبيعيًا، ومعاينة 360 و768 و1440px باللغتين، واختبار عنوان طويل من CMS وعند غيابه. لا انكسار غير مقصود أو حروف ملتصقة.

### NQ-006 — أسماء الوصول للأزرار ذات الأيقونات

**موضع العمل:** `_AIChatWidget.cshtml`، `_Navbar.cshtml`، أيقونات المشاركة بصفحات التفاصيل، أزرار الأدوات في layouts وviews الإدارة.

**الحل:** لكل button/link اسم يعبر عن الفعل باللغة الحالية عبر نص ظاهر أو sr-only أو aria-label. الأيقونة الزخرفية `aria-hidden`؛ أيقونة لا تحمل نصًا ليست اسمًا. استخدم button للإجراء وlink للتنقل. أضف aria-expanded/controls لعناصر الفتح، ورد focus إلى الزر عند الإغلاق، وEscape حيث يناسب الحوار. لا تجعل tooltip الوسيلة الوحيدة للتسمية.

**التحقق:** افتح وأغلق المحادثة والقائمة وبدّل اللغة والثيم بلوحة المفاتيح؛ افحص accessibility tree والأسماء، ولاحظ focus واضحًا ولا يوجد keyboard trap. اختبار AR وEN للأسماء التي تتغير ديناميكيًا.

### NQ-007 — تسميات فلاتر CRM وCMS

**موضع العمل:** `Views/CrmCompanies/Index.cshtml` و`CrmContacts/Index.cshtml` و`CrmLeads/Index.cshtml` و`CrmDeals/Index.cshtml`، وقوائم CMS والوسائط.

**الحل:** label مربوط بـid فريد لكل search/select/upload، ووصف مميز لكل زر أيقونة. placeholder ليس بديل label. حافظ على قيم الفلاتر بعد submit والتنقل، وأضف حالة focus ورسالة واضحة للنتائج الفارغة وزر إعادة تعيين. عرّف dialogs اختيار الوسائط بعناوين وأسماء أزرار مفهومة.

**التحقق:** التنقل بـTab، نطق اسم الفلتر وقيمته، تطبيق filter ثم next ثم العودة، وreset يعيد الحالة المتوقعة. لا IDs مكررة عند وجود أكثر من نموذج في الصفحة.

### NQ-008 — عقد موحد للحقول المطلوبة

**موضع العمل:** `Neurix/TagHelpers/RequiredLabelTagHelper.cs`، ViewModels في `Models/`، `_ValidationScriptsPartial.cshtml` والنماذج العامة والإدارية.

**الدليل المحلي:** TagHelper يرسم النجمة من metadata؛ هذا لا يثبت وجود native HTML required في input، وnon-nullable properties تؤثر على metadata.

**الحل:** اجعل الاختيارية صريحة في ViewModel. أضف required وaria-required عند ملاءمتهما لنوع input دون فرضهما على checkbox مثل IsPublished، واستخدم required مع Guid? إذا كان عدم الاختيار خطأ. اعتمد metadata نفسها للعلامة المرئية. تحقق أن client validation وserver validation متفقان؛ لا تُعطّل تحقق الخادم لتسوية اختلاف الواجهة.

**التحقق:** افحص HTML الناتج، وليس Razor فقط، ثم submit فارغ مع JS وبدونه، وتعديل request يدويًا. يجب رفض المطلوب وقبول الاختياري، وظهور نجمة واحدة ورسالة مفهومة. اختبر create وedit لأن default values قد تخفي الخطأ.

### NQ-016 — تجربة validation في التواصل

**موضع العمل:** `Contact.cshtml` و`ContactViewModel.cs` و`RequiredLabelTagHelper.cs` و`HomeController.Contact` وموارد الترجمة. يعتمد على NQ-013 وNQ-008.

**الحل:** أزل النجوم اليدوية الزائدة إذا كان TagHelper يضيفها. ضع validation summary مناسبًا ورسالة لكل حقل، مع aria-describedby وaria-invalid عند الخطأ؛ ركّز أول خطأ دون محو المدخلات. تحقق من إعادة تحميل ContentPage عند ModelState غير صالح. اختيار نوع الاستفسار له قيمة واضحة ورسالة translated. loading على زر الإرسال يمنع التكرار ويُفك عند الخطأ.

**التحقق:** empty submit، اسم شركة فارغ صالح، بريد خاطئ، طول زائد، خطأ خادم، والعودة للتصحيح. القبول: لا نجمتين ولا رسالة framework خامة ولا نجاح بعد فشل التخزين، ورسائل قابلة للقراءة في RTL.

### NQ-017 — تحديث Dashboard وفق الوظائف المتاحة

**موضع العمل:** `Views/CrmDashboard/Index.cshtml` و`CrmDashboardController.cs` وقواميس الترجمة.

**الحل:** راجع بطاقة Coming in later phases مقابل routes الفعلية وصلاحية الدور؛ Deals موجودة في `CrmDealsController`. انقل الميزة العاملة إلى إجراءات Dashboard المناسبة، وأبقِ المخطط فقط لما لم ينفذ. إن كانت هناك feature flags فعلية فاستخدم مصدرها نفسه؛ لا تُنشئ نظام flags كاملًا لتغيير عبارة ثابتة. الروابط الممنوعة على الدور لا تُعرض كوظيفة متاحة.

**التحقق:** Admin وCrmStaff؛ كل بطاقة تقود لصفحة متاحة، ولا ميزة موصوفة في الوقت نفسه كعاملة ومستقبلية، والنص العربي والإنجليزي متفقان.

### NQ-018 — محتوى مستقل لـPlus وClub

**موضع العمل:** `Views/Home/Plus.cshtml` و`Club.cshtml`، `HomeController.Plus/Club`، `CmsDivisionPageService.cs` و`CmsPageBandService.cs` و`CmsSeeder.cs`.

**الحل:** قارن slug وpageKey والـDTO والمحتوى المنشور والـfallback. أصلح mapping خاطئ إن وجد، ثم حرر وصف القيمة والجمهور والخدمات وCTA والصور لكل قسم على حدة بمحتوى معتمد. لا تستبدل نصًا موجودًا في قاعدة حقيقية بمجرد تشغيل seeder؛ احترم تحرير المشرف. لا يكفي تبديل اسم القسم داخل فقرة واحدة متكررة.

**التحقق:** تعديل Plus من CMS لا يغير Club والعكس، في AR/EN ومع CMS مفقود. وسع `CmsDivisionPageServiceTests.cs` و`CmsDivisionPagesControllerTests.cs` و`CmsPageBandServiceTests.cs` للتحقق من عزل المفاتيح.

### NQ-019 — تفاصيل حقيقية للمقالات والمشاريع

**موضع العمل:** `Views/Home/InsightDetail.cshtml` و`ProjectDetail.cshtml`، خدمات `CmsBlogPostService` و`CmsProjectService`، DTOs وeditors المقابلة.

**الدليل المحلي:** InsightDetail يعرض BodyEn أو SummaryEn عند غيابه؛ fallback مفيد تقنيًا لكنه لا يثبت اكتمال المقال أو ترجمة محتواه.

**الحل:** اعرض body الصحيح للغة مع سياسة واضحة لنقص الترجمة؛ ميّز summary عن body. جهز حقولًا ومحتوى معتمدًا: المشكلة والنطاق والحل والنتائج للمشروع، ومتن ومصادر للمقال؛ لا تختلق إحصاءات أو عملاء. أصلح editor/service mapping قبل إضافة حقول جديدة. نقّ HTML، واحمِ عرض slug بحيث لا يكشف مسودة عبر رابط مباشر. لا تُنشر مادة ناقصة باعتبارها دراسة حالة مكتملة.

**التحقق:** محتوى مختلف عن الملخص يُحفظ ويظهر، صور وروابط سليمة، AR/EN، missing slug وunpublished يعيدان السلوك الصحيح، وتجربة HTML غير آمن في بيئة اختبار. راجع الاختبارات الموجودة في `FullCmsServicesTests.cs` و`HomeControllerTests.cs` قبل إضافة اختبارات جديدة.

### NQ-023 — استكمال الترقيم والفرز والتصدير

**موضع العمل:** `CompanyService.cs` و`ContactService.cs` و`LeadService.cs` و`DealService.cs` وواجهاتها، `PagingDefaults.cs` و`PagedResult.cs`، Controllers قوائم CRM، `Crm*ListViewModels.cs`، `_CrmPagination.cshtml`.

**الدليل المحلي:** الترقيم الخادمي موجود، page size الافتراضي 20 والحد 100، ويوجد ترتيب ثابت مع Id؛ الـpartial يظهر فقط إذا تجاوزت الصفحات واحدة. لا تستنتج غياب pagination من قائمة قصيرة.

**الحل:**

1. جهز 45 سجلًا على الأقل للاختبار، وأثبت count والصفحات والفلاتر الحالية قبل التعديل.
2. أضف sort key وdirection بقائمة مسموحة على مستوى الخدمة، مع Id كفاصل تعادل. لا تحول نص العميل إلى SQL أو expression غير مقيد.
3. احتفظ بـsearch/status/company/owner/sort في كل روابط التنقل، وأعد page إلى 1 عند تغيير الفلتر. عالج page غير رقمية أو سالبة أو ضخمة، وصفحة أصبحت خارج المدى بعد الحذف. احسب الحدود قبل Skip لمنع overflow.
4. أضف export route محميًا في Controller المعني يعيد الملف عبر الخدمة، بنفس شروط البحث والصلاحيات، وليس أول صفحة فقط. حدّد سقفًا واضحًا للحجم؛ لا تقطع النتائج بصمت. عند تجاوز السقف أظهر طلب تضييق الفلاتر.
5. التصدير لا يكشف أسرارًا أو حقولًا مخفية. استخدم UTF-8 مناسبًا للعربية، وCSV quoting صحيحًا للفواصل والاقتباسات والأسطر، وحماية formula injection للقيم التي تبدأ بـ`= + - @` وفق طريقة التصدير. سجّل من صدر ومتى والعدد، دون نسخ البيانات إلى logs.

**التحقق:** 0/1/20/21/45 سجلًا، أسماء وتواريخ متطابقة، فرز تصاعدي/تنازلي، فلاتر مجمعة، page=int.MaxValue، حذف آخر سجل، وتصدير عربي بفواصل واقتباسات ونص يبدأ بصيغة. نفّذ SQL Server integration للترتيب والفلاتر؛ EF InMemory ليس إثباتًا لسلوك SQL.

### NQ-024 — إجراءات Contacts وDeals

**موضع العمل:** `CrmContactsController.cs` و`CrmDealsController.cs`، `Views/CrmContacts/` و`Views/CrmDeals/`، `ContactService.cs` و`DealService.cs`.

**الحل:** routes details/edit/delete موجودة في أجزاء الكود المفحوصة؛ أثبت موضع النقص المرئي أولًا. أظهر View/Edit/Delete حسب الدور في القوائم والتفاصيل والموبايل، وأعد استخدام routes الحالية. الحذف POST مع anti-forgery وتأكيد واضح وحذف منطقي وفق قواعد العلاقات، لا GET. افحص أن Contact تابع للشركة المحددة في Deal وأن السجلات المرتبطة موجودة وغير محذوفة. عند تعارض أو سجل محذوف أثناء التحرير أعد رسالة واضحة أو 404 ملائمة، لا exception خامًا.

**التحقق:** الأدوار المختلفة، ID غير موجود، تغيير company/contact داخل POST، delete مكرر، سجل مرتبط بصفقة، وظهور النتائج بعد الحذف. القبول ليس وجود زر فقط؛ يجب نجاح/رفض المسار نفسه حسب القاعدة.

### NQ-025 — مرفقات CRM بصلاحيات السجل

**طبيعة البند:** إضافة وظيفة إذا ثبت غيابها، وليست تعديل CSS. لا تساوِ مكتبة وسائط CMS العامة بمرفقات CRM الخاصة.

**إضافات مقترحة:** `CrmAttachmentsController.cs` في Controllers، `ICrmAttachmentService/CrmAttachmentService` وDTO في BLL، `CrmAttachment` في DAL وmigration لـCrmDbContext، partial للمرفقات داخل views السجلات. أسماء مقترحة وليست ملفات موجودة حاليًا.

**الحل:** ربط كل ملف بسجل واضح النوع والمعرف مع uploader/time/size/content type/storage key/status. تخزين خاص خارج `wwwroot/uploads` لأن `Program.cs` يقدم ذلك المسار للعامة. أسماء تخزين مولدة، allowlist محدودة، فحص signature والحجم وعدد الملفات ومساحة الحساب، وفحص برمجيات ضارة قبل إتاحة التنزيل. حالة quarantine حتى الفحص؛ لا ادعاء سلامة عند تعطل scanner. التنزيل من endpoint يراجع صلاحية السجل والملف في كل مرة، وContent-Disposition آمن وnosniff؛ لا تقبل storage path من العميل. عالج فشل حفظ DB بعد الرفع وتنظيف orphan files بإجراء مضبوط. حدد سياسة حذف واحتفاظ ونسخ احتياطي للمجلد الخاص.

**التحقق:** امتداد مزدوج وMIME مزور وملف ضخم واسم مسار traversal، تنزيل anonymous ومستخدم غير مخول، ملف لسجل محذوف، فشل الفحص، وفشل التخزين/DB. القبول: لا وصول مباشر للملف الثابت ولا تسريب بين السجلات، ودليل عمل الفحص؛ لا يكفي رفع ملف بنجاح.

### NQ-027 — Metadata خاصة بكل صفحة

**موضع العمل:** `_Layout.cshtml` و`HomeController.cs` وDTOs المحتوى والمشاريع والمقالات وخدماتها.

**الدليل المحلي:** Layout يستعمل `ViewData["MetaDescription"]` أو tagline عام، ويحتوي بعض Open Graph بالفعل. أكمل النقص ولا تضف tags مكررة.

**الحل:** title/description خاصان بالصفحة واللغة، canonical واحد مطلق مبني من public base URL موثوق، وog:url/title/description/image وTwitter card ملائمة. لا تستخدم Host غير موثوق لبناء canonical. حدّد URLs مستقرة للغات وhreflang إن كانت اللغة قابلة للعنونة؛ لا تولد روابط عربية لا يمكن إعادة فتحها بلغتها. لا تفهرس CRM/CMS/login/search الداخلية. اجعل صورة المشاركة عامة قابلة للتحميل، واستخدم بيانات CMS المعتمدة مع fallback خاص بالصفحة.

**التحقق:** افحص HTML الخادم قبل JS، وعدم تكرار canonical/description، وصحة اللغة والصورة والروابط، وبيانات article/project حسب نوع الصفحة. اربط الاختبار بـNQ-010؛ sitemap لا يصلح metadata تلقائيًا.

### NQ-031 — إزالة الفراغ غير المقصود قبل الفوتر

**موضع العمل:** `_Layout.cshtml`، الصفحة التي تُعاد فيها المشكلة، `wwwroot/css/neurix.css`، `site.js` و`neurix-sphere.js` إذا كان pin أو canvas مشاركًا.

**الحل:** انتظر اكتمال العرض ثم افحص box model للأقسام الأخيرة وmain وfooter وScrollTrigger pin spacers. اختبر CMS فارغًا وممتلئًا وأقسامًا unpublished. عدّل المسؤول الفعلي: padding/min-height أو section خالٍ أو spacer لم يُنظف. لا تخفِ العيب بـnegative margin شامل أو إخفاء overflow يؤدي إلى قص محتوى. أبقِ فوتر الصفحات القصيرة في موضع منطقي.

**التحقق:** screenshots كاملة في desktop/mobile وAR/EN مع JS معطل وreduced motion ومع فشل تحميل مكتبة الحركة. القبول: لا مساحة وهمية ولا تداخل أو قص أو قفزة footer أثناء التحميل.

### NQ-034 — مسؤول متابعة أو قائمة استقبال واضحة

**موضع العمل:** `LeadService.CaptureWebsiteEnquiryAsync`، `CrmLeadFormViewModel.cs`، `CrmLeadsController.cs`، DTOs، وإعداد BLL مناسب لقاعدة assignment.

**الحل:** وجود Unassigned ليس خطأ قاعدة بيانات تلقائيًا؛ المطلوب ضمان المتابعة. اعتمد مالكًا افتراضيًا صالحًا أو قاعدة توزيع معتمدة، وتحقق من نشاطه ودوره. إذا لم يوجد مالك مناسب احفظ الاستفسار في قائمة استقبال Unassigned واضحة مع إشارة متابعة داخلية، ولا تفشل التقاط lead أو تسنده لأول admin اعتباطيًا. أي إشعار خارجي يحتاج قناة معدّة ومعتمدة، مع إعادة محاولة لا تكرر Lead. احفظ وقت الإنشاء/التعيين وسجل من غيّره، وأضف فلتر غير المسند.

**التحقق:** owner صالح/معطل/محذوف أو إعداد مفقود، وصول طلبين متزامنين، تبديل الإسناد بصلاحية صحيحة، تعطل الإشعار. القبول: كل lead محفوظ ويمكن اكتشافه ومتابعته، دون إسقاط بيانات عند فشل التوزيع.

## 7. الإصلاحات منخفضة الأولوية

### NQ-009 — صفحة 404 مفهومة مع status صحيح

**موضع العمل:** `Program.cs` و`HomeController.cs` و`Views/Shared/` أو `Views/Home/` لإضافة view مناسبة.

**الحل:** عالج المسارات غير المعروفة والـNotFound الناتج عن slug أو ID عبر status-code handling مناسب دون تحويلها إلى HTTP 200 أو إخفاء أخطاء 500. صمم صفحة بسيطة مترجمة بروابط الرئيسية والتواصل والعودة الآمنة؛ استخدم البحث فقط إن كان موجودًا فعلًا. لا تعرض stack trace أو تفاصيل النظام. تجنب recursion إذا فشلت صفحة الخطأ نفسها.

**التحقق:** مسار عشوائي ومقال غير موجود وasset مفقود وطلب API؛ الـHTML يناسب صفحات الموقع، وAPI لا يحصل على صفحة نجاح HTML. 404 يبقى 404، واختبر AR/EN وanonymous/authenticated.

### NQ-010 — robots.txt وsitemap.xml

**موضع العمل:** `Neurix/wwwroot/robots.txt` كإضافة مقترحة للملف الثابت، وController صغير في `Controllers/` لتوليد sitemap عند الحاجة، وخدمات CMS الحالية لاختيار المنشور.

**الحل:** robots صالح مع رابط sitemap مطلق للنطاق الإنتاجي. sitemap يجمع صفحات عامة وslugs منشورة فقط، مع XML escaping وlastmod حقيقي إن توفر. استبعد CMS/CRM/login والمسودات والروابط التجريبية وquery filters. احترم canonical في NQ-027. عالج تعطل DB دون إعادة sitemap ناقص ثم تخزينه طويلًا كأنه صحيح؛ يمكن الاحتفاظ بآخر نسخة صالحة مع سياسة تحديث واضحة. robots وسيلة توجيه محركات، وليس حماية وصول.

**التحقق:** GET مباشر يعيد 200 وcontent types صحيحة، XML قابل للقراءة وروابط مطلقة تعمل، عدم وجود host محلي أو unpublished، وتحديث القائمة بعد النشر/إلغاء النشر. افحص النتيجة بعد publish/container، لا dev فقط.

### NQ-011 — توطين العرض دون كسر إدخال القيم

**موضع العمل:** `Localization/CmsLanguageExtensions.cs`، badges، `_CrmPagination.cshtml`، views المقالات وقوائم CRM والتفاصيل.

**الدليل المحلي:** formatting culture مثبتة عمدًا على الإنجليزية لحماية binding للأرقام والتواريخ؛ UI culture تتغير. لا تحول CurrentCulture عالميًا للعربية كحل سريع.

**الحل:** استخدم ثقافة العرض المناسبة صراحة أو `Intl.DateTimeFormat/NumberFormat` للعرض فقط؛ حافظ على UTC في التخزين وقيمة ISO لمدخلات date وعلى decimal قابل للربط. اختر المنطقة الزمنية والتقويم المطلوبين واعرضهما باتساق دون افتراضهما من اللغة. ترجم التصنيفات والحالات وقوالب العد مثل Page of records؛ لا تترجم قيمة المستخدم الأصلية.

**التحقق:** تواريخ قرب منتصف الليل، تاريخ فارغ، مبالغ عشرية وصفر وكبيرة، AR→EN→AR، حفظ المبلغ بعد العرض بالعربية دون تغير قيمته. اختبارات تقارن القيمة المخزنة وليس شكل النص فقط.

### NQ-012 — قوائم صالحة للموبايل

**موضع العمل:** views قوائم CRM وpartials مشتركة و`neurix.css`/Tailwind source بحسب النمط الحالي.

**الحل:** اعتمد عرض بطاقات صغيرًا أو أعمدة ذات أولوية مع تفاصيل قابلة للفتح. احتفظ بالإجراءات والحالة والاسم الأساسي متاحة، ولا تعتمد على إخفاء الأعمدة الضرورية. إذا استمر horizontal scroll فليكن داخل الجدول مع إشارة واضحة ومنطقة يمكن الوصول إليها بلوحة المفاتيح؛ ليس تمرير الصفحة كلها. تجنب IDs مكررة بين نسختي desktop/mobile أو ازدواج المحتوى لقارئ الشاشة.

**التحقق:** 320/360/390/768px، RTL وLTR، أسماء عربية طويلة وبريد طويل وبيانات فارغة، zoom 200%، وأزرار view/edit/delete والفلاتر والتنقل قابلة للاستخدام دون قص.

### NQ-020 — مصدر واحد لبريد التواصل

**موضع العمل:** `_Footer.cshtml` و`Views/Home/Contact.cshtml`، `CmsCompanyProfileService.cs` و`CmsSiteSettingService.cs`، fallback وseed للمحتوى.

**الحل:** اعتمد البريد من إعداد CMS واحد مناسب؛ إن اختلف بريد المبيعات والدعم فاشرح وظيفة كل منهما. لا تختَر أي عنوان من التقرير كأنه معتمد حاليًا. اجعل النص وmailto متطابقين، وتخلص من fallback متناقض، واحترم اللغة دون تغيير البريد. تحقق من mailbox وتوجيهه مع مسؤول التشغيل؛ لا ترسل رسالة اختبار خارجية إلا بتفويض مناسب.

**التحقق:** تعديل المصدر ينعكس في التواصل والفوتر وكل CTA، mailto لا يحتوي whitespace خاطئة، ومع غياب CMS لا يظهر عنوان قديم مضلل. لا يغلق اختبار UI وحده مسألة التسليم البريدي.

### NQ-021 — تهيئة JavaScript مرة واحدة

**موضع العمل:** `_Layout.cshtml` و`_Navbar.cshtml` و`_Footer.cshtml` وScripts sections و`wwwroot/js/site.js` و`neurix-sphere.js` و`ai-chat.js`.

**الحل:** اجرد script tags وhandlers قبل الحذف؛ قد تتكرر logs دون تحميل الملف مرتين. امنع تحميل الملف في layout وview معًا، واجعل تهيئة widget idempotent عند الحاجة، ونظف listeners/observers/animation loops عند الإغلاق وإعادة الإنشاء. حافظ على ترتيب dependencies وتعامل مع غياب CDN دون إخفاء المحتوى. أزل console logs التشخيصية من مسار الإنتاج دون إزالة error handling النافع.

**التحقق:** Network يظهر طلب script المتوقع، click واحد يطلق action واحد، فتح/إغلاق chat مرات لا يضاعف الإرسال أو الميكروفون، وتبديل اللغة لا يكرر event listeners. لا تكتفِ بصمت Console.

### NQ-030 — دلالات الصفحة وأيقونة الموقع

**موضع العمل:** `_Layout.cshtml` و`_Footer.cshtml` و`_Navbar.cshtml` وlayouts الإدارة وصفحاتها وملفات favicon.

**الحل:** landmark واحد رئيسي لـmain، وheader/nav/footer دلالية، وترتيب headings صحيح وskip link، وصور ذات alt مناسب. افحص main الداخلي في Privacy لأنه يقع داخل main الموجود في layout. تأكد من وجود favicon فعلية بمسار وصيغة صحيحة؛ غيابها بند هوية/عرض منفصل عن دلالات الوصول. لا تضف role مكررًا إلى عنصر HTML يملك الدلالة أصلًا.

**التحقق:** accessibility tree وkeyboard وفحص آلي ملائم مع مراجعة يدوية، logo/صور زخرفية وlang/dir صحيحان، وأيقونة الموقع تعيد 200 بعد النشر. لا تدّع مطابقة WCAG كاملة من إصلاح عدد محدود من العناصر.

### NQ-035 — دورة مراجعة ونشر وسجل تدقيق

**موضع العمل:** `Neurix.BLL/Services/Cms/` وDTOs، `Neurix.DAL/Models/Cms*.cs` و`CmsDbContext.cs` و`Migrations/Cms/`، `CmsRoles.cs`، Controllers وeditors وDashboard الخاصة بـCMS.

**حد الدليل:** ظهور Published وعدم ظهور سجل في الواجهة لا يثبت غياب أي audit خلفي؛ افحص الموجود قبل إنشاء بديل.

**الحل:** عرّف انتقالات Draft→InReview→Published مع reject→Draft وunpublish، وصرّح بالمراجعة والنشر في الخادم لا بإخفاء الزر فقط. يمكن اعتماد Admin كناشر أولي وCmsEditor للمسودات إذا اعتمد هذا الفصل؛ الثابت وحده لا يمنح صلاحية. أضف سجلًا غير قابل للتحرير المعتاد يتضمن actor/time/action/entity/version وتغيرات آمنة محدودة، دون أسرار. عالج تحرير عنصر منشور بنسخة مسودة حتى لا يتغير المنشور قبل المراجعة، وامنَع تعارض مراجعتين عبر concurrency/version. أي Published قائم يُرحّل مع تمييز أنه محتوى قديم بلا سجل مراجعة تاريخي؛ لا تختلق reviewer أو وقت اعتماد. القراءة العامة تعتمد النسخة المعتمدة فقط. سجل تغيير الحالة والتدقيق في transaction واحدة.

**التحقق:** editor لا ينشر عبر POST معدل، reviewer لا يعتمد نسخة تغيرت بعد عرضها، رفض ثم تعديل ثم اعتماد، إلغاء نشر، preview محمي، وتحقق audit في كل انتقال. اختبر كل نوع محتوى مشمول؛ وجود workflow لخدمة واحدة لا يغلق البند لكل CMS. حدّث `CmsDashboardControllerTests.cs` واختبارات الخدمات/controllers المتأثرة.

## 8. مراجعة المشروع كاملة أثناء التنفيذ

هذه بوابات منع ارتداد مشتركة، وليست بنودًا إضافية منسوبة إلى PDF ولا إثباتًا بوجود عيوب جديدة في كل مسار.

| المجال | ما يجب الحفاظ عليه أو التحقق منه |
|---|---|
| الرئيسية | Hero وHuman Vision وPioneers وPillars وDivisions وAI Engineering وEthics وCTA وقوائم Portfolio/Insights؛ منشور/فارغ/ترجمة ناقصة |
| صفحات الأقسام | Labs/Technology/HQ/Plus/Club/AI؛ content bands وCTA والصور والمفاتيح الصحيحة؛ غياب CMS لا ينتج صفحة غير قابلة للاستخدام |
| About/Contact/Privacy | النص الطويل والصور والفريق وبيانات التواصل وfallback وتطابق GET مع POST غير الصالح |
| Insights/Portfolio | القائمة والتفاصيل وslugs والنشر واللغة والصور وmetadata |
| الدخول والخروج | lockout/rate limit/returnUrl/cookies/anti-forgery وعدم إظهار تفاصيل البنية التحتية |
| Companies | create/edit/details/search/status/pagination/soft-delete ومنع حذف شركة ذات ارتباطات فعالة |
| Contacts | تبعية الشركة وصحة البيانات والإجراءات والفرز والبيانات المحذوفة |
| Leads | التقاط الموقع وإنشاء الإدارة والفلاتر والإسناد والتحويل وعدم ازدواج السجلات |
| التحويل | atomicity واختيار الشركة واكتشاف التكرار وصلاحية السجلات وطلبان متزامنان؛ لا تعتبر مجرد فحص الحالة قبل Save ضمانًا للتزامن |
| Deals | القيمة والمرحلة والشركة وجهة الاتصال والإسناد؛ تحقق Enum.IsDefined أو validation مكافئ عند حدود الكتابة |
| CMS company profiles | بيانات العلامة والصور والألوان وبريد التواصل؛ التغيير في CMS يصل إلى العرض العام |
| CMS services/projects/blog/team/testimonials | CRUD واللغة والترتيب والنشر والـslug والوسائط والحذف |
| CMS settings/social/menu | الروابط والبروتوكولات المسموحة والترتيب وتوابع النشر؛ منع javascript: URLs والروابط المضللة |
| CMS home/division/content/page bands | المفاتيح والـfallback وعدم استبدال محتوى العميل عند seed |
| CMS media | تحقق نوع/حجم/اسم الملف وروابط استخدامه، وعدم حذف أصل مستخدم بلا قرار واضح |
| AI text/voice | state/timeouts/limits/retry وسلامة العرض وتنظيف الموارد؛ لا إدراج HTML غير منقّى من رد النموذج |
| Localization | UI culture مقابل formatting culture، مفاتيح الترجمة والرسائل وRTL واستمرار اللغة |
| CSS/JS | مصدر Tailwind والناتج، CDN failures وreduced motion، وعدم تكرار initialization |
| DAL | query filters وFK/indexes وprecision والأطوال والتواريخ UTC وmigrations لكل context |
| التشغيل | startup migration/seed وhealth والـlogs وuploads persistence وTLS/proxy/headers |
| التوثيق | تحديث README لتطابق npm/الملفات/الأوامر الفعلية؛ لا تثبيت أرقام tests أو endpoints غير مقاسة |

### 8.1 قواعد خاصة بقاعدة البيانات

- لا تغير migration منشورة. أضف migration باسم يشرح schema change، وراجع SQL الناتج قبل التطبيق.
- لا تفترض أن اختبار EF InMemory يفحص FK أو unique indexes أو transactions أو collation مثل SQL Server.
- أي إضافة لازمة لـNewsletter/Attachments/Workflow تحتاج نموذج بيانات وmigration واختبار rollback/restore وخطة backfill قبل النشر.
- نفّذ تنظيف البيانات أو backfill بإجراء قابل للمراجعة مع counts ونسخة احتياطية، لا داخل طلب page عادي ولا seeder يعيد كتابة كل سجل.
- عند تعدد instances، حدد جهة واحدة مسؤولة عن migration في عملية النشر لمنع سباق startup. مراجعة هذا السلوك تشغيلية وليست دعوة إلى تغيير المعمارية كلها.
- لا تدخل إصلاحات بدمج موازٍ لاستعلامات EF على نفس DbContext؛ الاستدعاءات الحالية المتتابعة لا تتحول إلى `Task.WhenAll` دون فحص lifetime وفصل السياقات.

### 8.2 قواعد المحتوى وHTML

- كل نص قابل للإدارة يُعدل من مصدر CMS المناسب مع fallback متوافق؛ لا تستبدل المحتوى الديناميكي بـhardcoded copy لتجميل لقطة شاشة.
- حافظ على فصل النص الإنجليزي والعربي في DTOs والكيانات؛ نقص الترجمة له سياسة عرض معلنة.
- المحتوى القانوني ودراسات الحالة والأرقام وبريد التشغيل يحتاج اعتماد مالكه؛ لا تملأ الفراغ بادعاءات غير موثقة.
- أي HTML غني معروض بـHtml.Raw أو parser للـMarkdown يحتاج sanitization حقيقيًا، لا استبدالًا نصيًا بسيطًا. حدّد tags/attributes/protocols المسموحة واختبر event handlers وروابط script.

## 9. ترتيب التنفيذ والاعتماديات

حافظ على خطورة المصدر كما هي. الترتيب التالي ينظم التنفيذ ولا يعيد تصنيف البنود.

| المرحلة | البنود | ناتج المرحلة وبوابة الانتقال |
|---|---|---|
| 0. Baseline | الجميع | نسخة مصدر معلومة، قاعدة اختبار، إعادة إنتاج وتحديد ما تغير عن التقرير، screenshots وHTTP evidence |
| 1. الحرج | NQ-013، NQ-029 | إرسال contact مثبت التخزين؛ صلاحية وتجديد TLS مثبتان في جهة الاستضافة |
| 2. الحماية التشغيلية | NQ-003، NQ-022، NQ-028، NQ-032، NQ-033 | إعدادات واختبارات وصول وحدود وسجل قرارات بيانات |
| 3. مسارات الثقة والتحويل | NQ-001، NQ-002، NQ-014، NQ-015، NQ-026 | لغة مستقرة، سياسات معتمدة، اشتراك صحيح، AI صادق الحالة، حساب قابل للإدارة |
| 4. النماذج والمحتوى | NQ-004، NQ-005، NQ-006، NQ-007، NQ-008، NQ-016، NQ-017، NQ-018، NQ-019 | validation وترجمة ووصول ومحتوى متسق |
| 5. تشغيل CRM | NQ-023، NQ-024، NQ-025، NQ-034 | قوائم وإجراءات ومرفقات وإسناد مع اختبارات SQL وصلاحيات |
| 6. الظهور والتجربة | NQ-027، NQ-031، NQ-009، NQ-010، NQ-011، NQ-012، NQ-020، NQ-021، NQ-030 | SEO و404 وموبايل وRTL وJS بلا ارتداد |
| 7. حوكمة المحتوى | NQ-035 | مراجعة ونشر وتدقيق لكل أنواع المحتوى المتفق عليها |
| 8. التسليم | الجميع | أدلة إغلاق فردية ثم regression نهائي وخطة نشر واستعادة |

**اعتماديات لا تُتجاهل:**

- NQ-008 وNQ-016 مرتبطان بعقد NQ-013؛ تجنب ثلاثة حلول مختلفة لنفس Required.
- NQ-001 أساس NQ-004/NQ-011؛ توحيد لغة الحالة يمنع ترقيع كل صفحة منفردة.
- NQ-003/NQ-028/NQ-029 تشترك في ضبط HTTPS والوكيل؛ الاختبار الخارجي ضروري.
- NQ-019/NQ-002/NQ-027/NQ-010 تتشارك معنى «منشور» والروابط العامة؛ راجعها مجددًا بعد NQ-035.
- NQ-023 وNQ-012 يجب أن يحافظا على نفس الفلاتر والإجراءات في desktop/mobile.
- لا تجعل انتظار محتوى قانوني أو وصول استضافة يمنع إنجاز الإصلاحات المستقلة؛ سجّل الجزء المنتظر بدقة.

## 10. خطة الاختبار والأدلة

### 10.1 البيئة والبيانات

1. استخدم قواعد SQL Server مخصصة للاختبار، مع Admin وCrmStaff وحساب غير مصرح عند الحاجة؛ الأدوار الإضافية تُنشأ فقط إن دخل workflow الجديد في النطاق.
2. أنشئ 45 سجلًا على الأقل لكل قائمة تخضع لاختبار الترقيم، بأسماء عربية وإنجليزية وتواريخ متساوية وحالات متعددة وروابط سليمة.
3. جهز محتوى منشورًا ومسودة ومترجمًا وناقص الترجمة وصورة مفقودة ونتائج فارغة.
4. لا تنسخ بيانات العملاء أو كلمات المرور أو ملفاتهم إلى fixtures أو screenshots. عند الحاجة إلى دليل من بيئة فعلية أخفِ القيم الشخصية.
5. سجّل اسم البيئة ونسخة الكود ووقت الاختبار واللغة والعرض والدور مع كل دليل.

### 10.2 مستويات التحقق

| المستوى | ما يثبته | ما لا يثبته |
|---|---|---|
| Build | صحة compilation وربط المشاريع | صحة تجربة الاستخدام أو حفظ البيانات |
| xUnit services/controllers | فروع وقواعد سلوك محددة | middleware/model binding/attributes عند استدعاء Controller مباشرة |
| HTTP integration | routing وbinding وauth وanti-forgery وstatus | layout وصحة JS بالكامل |
| SQL Server integration | قيود واستعلامات ومعاملات وتزامن فعلية | صحة الإنتاج تلقائيًا |
| Browser QA | RTL/responsive/focus/interaction/DOM/screenshots | صحة قاعدة البيانات بمجرد ظهور success |
| External deployment checks | TLS/headers/cookies/assets/health على النسخة المنشورة | نجاح سيناريوهات لم تُنفذ |

اختبارات HTTP قد تحتاج إضافة اعتماد اختبار مناسب إلى مشروع الاختبارات الحالي؛ افحص الموجود أولًا، ولا توصف مكتبة غير موجودة بأنها جاهزة. لا تحتاج تعديلات النصوص البسيطة إلى اختبار يطابق السطر نفسه؛ التقط تحققًا بصريًا مناسبًا بدل اختبار شكلي.

### 10.3 الاختبارات القائمة التي يجب الاستفادة منها

| الملفات الموجودة | مجال التوسيع |
|---|---|
| `Neurix.Tests/HomeControllerTests.cs` | Contact success/failure، مسارات المحتوى والـfallback |
| `Neurix.Tests/CmsContentPageServiceTests.cs` و`CmsContentPagesControllerTests.cs` | صفحات السياسات والعربية والنشر |
| `Neurix.Tests/CmsDivisionPageServiceTests.cs` و`CmsDivisionPagesControllerTests.cs` | فصل Plus وClub وارتباط المحتوى |
| `Neurix.Tests/CmsPageBandServiceTests.cs` و`CmsPageBandsControllerTests.cs` | مفاتيح sections وبياناتها |
| `Neurix.Tests/CmsLocalizationAuditTests.cs` | مفاتيح الترجمة والرسائل |
| `Neurix.Tests/CmsHomepageEditableCoverageTests.cs` و`E2EHomepageSectionsIntegrationTests.cs` | بقاء CMS قابلًا للتحرير ووصوله للعرض؛ اسم E2E لا يغني عن اختبار متصفح |
| `Neurix.Tests/CmsMediaUploadHelperTests.cs` و`CmsMediaAssetUsageTests.cs` | وسائط CMS؛ لا تُستخدم كإثبات لحماية مرفقات CRM الجديدة |
| `Neurix.Tests/CmsDashboardControllerTests.cs` و`CmsSeederTests.cs` | counts الحقيقية وseed وحالة النشر |
| `Neurix.Tests/CardGridCssTests.cs` | قواعد CSS القائمة؛ مع فحص بصري فعلي |

اختبارات مقترحة عند تنفيذ نطاقها: `ContactSubmissionIntegrationTests`، `CrmAuthenticationIntegrationTests`، `CrmProfileIntegrationTests`، `CrmListQueryTests`، `NewsletterServiceTests`، `CrmAttachmentAuthorizationTests`، `CmsPublishingWorkflowTests`. تُضاف داخل `Neurix.Tests/` وفق convention الموجود، ولا يُدّعى نجاحها قبل إنشائها وتشغيلها.

### 10.4 مصفوفة المتصفح

- صفحات الموقع: الرئيسية، الأقسام الستة، About، Contact، Privacy/Terms/Security، Insights/Portfolio list/detail، مسار مفقود.
- لوحات التحكم: Login/Dashboard ثم list/create/edit/details لكل كيان CRM، ثم CMS editor والنشر والوسائط.
- الأوضاع: AR وEN، LTR وRTL، الثيمات المستخدمة، 360 و390 و768 و1440px؛ أضف 320px للقوائم والنماذج.
- الحالات: loading/empty/success/validation/error/unauthorized/not-found، شبكة بطيئة وفشل endpoint واعتماد خارجي محجوب.
- التفاعل: keyboard وfocus وEscape وback/forward وrefresh وفتح مباشر، وzoom 200% وreduced motion للأجزاء المتحركة.
- بعد كل navigation أو تبديل لغة انتظر العرض الفعلي وافحص DOM وscreenshot حديثين؛ لا تسجل «صفحة فارغة» من snapshot مبكر فقط.

### 10.5 حزمة إثبات لكل بند

```text
Bug: NQ-xxx
Source page:
Environment / code version / date:
Role / language / viewport:
Before: reproduction steps + actual behavior
Expected:
Cause: confirmed evidence, or remaining hypothesis
Changed files:
Migration / configuration / content dependency:
Checks run + exact result:
Browser / HTTP / database evidence:
Remaining limitation:
Status: Reproduced / In progress / Ready for QA / Verified / Blocked / Not reproducible
Owner / verifier:
```

حالة Not reproducible لا تعني تجاهل البند: اذكر خطواته ونسخة الكود والدليل وسبب اختلافه المحتمل. حالة Blocked تذكر الاعتماد المحدد والجزء المنجز، لا عبارة عامة «ينتظر مراجعة».

## 11. أوامر العمل على النسخة المحلية

الأوامر التالية مقترحة للتنفيذ لاحقًا من جذر المشروع. لم تُشغّل ضمن إعداد الوثيقة، ولا تمثل نتائج نجاح. افحص إعداد الاتصال المعتمد دون طباعة الأسرار قبل بدء التطبيق أو أي أمر قاعدة بيانات.

### 11.1 فحص الأدوات والبناء

```powershell
dotnet --info
node --version
npm --version
dotnet restore .\Neurix.slnx
npm --prefix .\Neurix ci
npm --prefix .\Neurix run build:css
dotnet build .\Neurix.slnx --configuration Release --no-restore
dotnet test .\Neurix.Tests\Neurix.Tests.csproj --configuration Release --no-build --logger "trx;LogFileName=neurix-qa.trx"
```

`npm ci` يعتمد على lockfile الموجود؛ لا تحدث إصدارات الحزم بلا سبب. `--no-build` صالح بعد build ناجح بنفس Configuration. عند تغيير الكود أعد build قبل test بهذا الخيار.

### 11.2 التشغيل المحلي

بعد ضبط اتصال اختبار معزول:

```powershell
dotnet run --project .\Neurix\Neurix.csproj --launch-profile https
```

راجع `Neurix/Properties/launchSettings.json` للمنافذ الفعلية بدل افتراضها. لا تستخدم HTTPS cookie checks على HTTP. إعداد Gemini اختياري لتصفح الموقع، لكنه مطلوب لاختبار التكامل الحقيقي للمحادثة.

### 11.3 Migrations عند وجود schema change فقط

مثالان بديلان حسب السياق المتأثر؛ لا تنشئ migration فارغة ولا تطبق كليهما تلقائيًا:

```powershell
dotnet ef migrations add QaCrmChanges --project .\Neurix.DAL --startup-project .\Neurix --context CrmDbContext --output-dir Migrations
dotnet ef migrations add QaCmsChanges --project .\Neurix.DAL --startup-project .\Neurix --context CmsDbContext --output-dir Migrations/Cms
```

استخدم اسمًا أدق للتغيير الحقيقي. بعد مراجعة migration واتصال البيئة، توليد SQL للمراجعة:

```powershell
dotnet ef migrations script --idempotent --project .\Neurix.DAL --startup-project .\Neurix --context CrmDbContext --output .\qa-crm-migrations.sql
dotnet ef migrations script --idempotent --project .\Neurix.DAL --startup-project .\Neurix --context CmsDbContext --output .\qa-cms-migrations.sql
```

النشر إلى قاعدة فعلية مرحلة تشغيل منفصلة بعد backup ومراجعة SQL. لا تشغّل database update على connection غير معروف.

### 11.4 تحقق نشر الحاوية

```powershell
npm --prefix .\Neurix run build:css
docker build -t neurix-mvc:qa .
```

Dockerfile الحالي لا يشغّل npm. إذا تغيّرت Styles أو classes فتأكد أن CSS الناتج داخل build context ونسخة publish؛ عند تعديل pipeline لاحقًا اجعل توليده حتميًا. تشغيل الصورة يحتاج إعداد قاعدة اختبار، واستمرار ملفات uploads ومفاتيح جلسات المصادقة حسب بيئة التشغيل. لا تضع كلمات مرور في أوامر منسوخة إلى تقرير.

## 12. النشر والاستعادة

1. **قبل النشر:** راجع diff والصلاحيات والأسرار، سجل نسخة الكود، نفّذ الاختبارات اللازمة، واعتمد المحتوى والإعدادات اللازمة للبنود التشغيلية.
2. **البيانات:** snapshot/backup قابل للاستعادة، migration متوافق قدر الإمكان مع النسخة السابقة، وإجراء backfill مستقل يمكن مراجعته.
3. **الملفات:** uploads العامة والخاصة على تخزين مستمر مناسب مع صلاحيات ونسخ احتياطي، لا filesystem حاوية مؤقت وحده.
4. **التهيئة:** public base URL وcookies وproxy وTLS وemail/scanner/AI حسب الوظائف المفعلة؛ secret store خاص بالبيئة، دون وضع قيمة فعلية في Markdown.
5. **الإطلاق:** طبّق migration عبر المسار التشغيلي المعتمد، انشر نسخة التطبيق، ثم smoke على الصفحات الحرجة والدخول والتواصل وحفظ/قراءة CMS.
6. **بعد الإطلاق:** تحقق خارجي من TLS/headers/Set-Cookie/404/robots/sitemap/CSS/uploads، وراقب 5xx وفشل حفظ Leads وفشل الاشتراك والمرفقات والمحادثة.
7. **التراجع:** حدد شروطه مسبقًا مثل تعذر الدخول أو فقد حفظ الاستفسار أو فشل migration. ارجع إلى صورة معلومة متوافقة مع schema؛ لا تعتمد على down migration مدمر. استعادة البيانات تحتاج خطة وفهم الكتابات اللاحقة للنسخة الاحتياطية.
8. **الإغلاق:** اكتب صراحة أين اختبر الإصلاح: محلي أو staging أو production. اجتياز المحلي لا يغلق بند شهادة الإنتاج أو تسليم البريد أو إعداد proxy الخارجي.

## 13. تعريف الانتهاء وشروط قبول التسليم

- [ ] جرى التعامل مع كل NQ-001 إلى NQ-035 دون إسقاط، مع الاحتفاظ بخطورة وحالة مصدر التقرير.
- [ ] كل بند له إعادة إنتاج أو دليل موثق على عدم ظهوره في النسخة الحالية.
- [ ] الحل في الطبقة القائمة الصحيحة؛ لا إعادة هيكلة غير لازمة ولا business logic في Razor/Controller.
- [ ] لا نجاح واجهة دون نجاح تخزين أو عملية حقيقية قابلة للإثبات.
- [ ] النماذج تتحقق على الخادم، والأدوار وanti-forgery والروابط الحساسة مختبرة عبر HTTP حيث يلزم.
- [ ] العربية والإنجليزية وRTL والموبايل والرسائل والحالات الفارغة جرى فحصها بصريًا.
- [ ] اختبارات SQL Server للتغيرات التي تعتمد على constraints/transactions/query behavior، لا EF InMemory وحده.
- [ ] المحتوى والسياسات والمراسلات وشهادة TLS والفحص الأمني للملفات لها اعتماد ودليل مناسب لنوعها.
- [ ] لم تُنشر مسودات أو بيانات اختبار أو أسرار أو ملفات CRM خاصة للعامة.
- [ ] CSS المبني وموارد الترجمة والـassets موجودة بعد publish، وملفات المستخدمين لا تُفقد بإعادة تشغيل الحاوية.
- [ ] README والأوامر وملاحظات النشر تطابق النسخة النهائية، وأي اعتماد متعذر مسجل مع مالكه.
- [ ] **لا تعليقات برمجية جديدة إلا لضرورة قصوى مثبتة؛ لا شرح سطري ولا XML boilerplate ولا عناوين مراحل ولا كود معلّق. هذا شرط قبول إلزامي.**
- [ ] التسليم يحتوي قائمة الملفات المعدلة، نتائج الاختبارات الفعلية، صورًا/HTTP evidence حيث يلزم، migrations/config المطلوبة، وحدود التحقق.

## 14. تعليمات جاهزة للمطور أو مساعد البرمجة

> اقرأ هذه الوثيقة مع الملفات الحالية قبل التنفيذ. عالج البنود واحدًا واحدًا مع الحفاظ على ASP.NET Core MVC والطبقات Neurix/Neurix.BLL/Neurix.DAL ومشروع Neurix.Tests. أعد إنتاج البند، وحدد سببه، ثم نفّذ أصغر تغيير متكامل يصلح السلوك ويحافظ على بقية التدفق. أعد استخدام الخدمات وDTOs وlayouts والترجمة والاختبارات الموجودة. لا تعِد بناء ميزة موجودة لأن التقرير وصفها بأنها ناقصة. لا تعتبر توصيات PDF تفويضًا لتغيير الإنتاج أو حذف البيانات.
>
> **ممنوع الإكثار من comments نهائيًا. اكتب كودًا واضحًا بالأسماء والتقسيم، ولا تضف تعليقًا إلا لسبب تقني غير بديهي وضروري جدًا لا يُفهم من الكود؛ بأقصر صياغة ممكنة. الشرح التفصيلي مكانه الوثائق والتسليم. لا تقلد التعليقات المطولة القديمة، ولا تكتب XML summaries لكل عضو أو عناوين للخطوات أو كودًا معطلًا.**
>
> لا تغلق البند بعد compilation فقط. اختبر السلوك المتأثر بالطريقة المناسبة، مع AR/EN وRTL والموبايل عند تغيير الواجهة، وHTTP/SQL عند تغيير validation أو الصلاحيات أو التخزين. عند اعتماد خارجي غير متاح، أكمل الأجزاء المستقلة وسجّل ما ينتظر التهيئة بدقة. في التسليم اذكر ما تغير وما اختبر بالفعل وما بقي، دون ادعاء production verification من اختبار محلي.
