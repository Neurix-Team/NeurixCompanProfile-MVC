# تقييم جاهزية Neurix للإنتاج

تاريخ آخر تحقق محلي: 23 سبتمبر 2026.

**القرار: المشروع قابل للتجهيز للإنتاج، لكن إعداد التشغيل الحالي ليس جاهزًا للنشر العام.** نجاح البناء والاختبارات المحلية لا يُثبت اكتمال حماية وتشغيل بيئة الإنتاج.

## ما تم التحقق منه

| الفحص | النتيجة | الحدود |
|---|---|---|
| بناء Release واختبارات المشروع | 258 ناجحًا، صفر فشل، صفر تخطٍّ | اختبارات محلية؛ عدة اختبارات تعتمد EF InMemory، وليست اختبار حمل أو فحص SQL Server الفعلي |
| حزم .NET المباشرة والمتعدية | لا تنبيهات ثغرات من مصادر NuGet المستخدمة وقت الفحص | لا يشمل مراجعة شاملة للكود أو JavaScript المورّد داخل wwwroot |
| npm package-lock | صفر ثغرات معلنة | يعتمد ملف القفل الخاص بأدوات بناء CSS |
| الصفحات العامة وrobots/sitemap | استجابات HTTP 200 محليًا | ليست نتيجة اختبار استضافة فعلية أو شبكة هاتف |
| مسارات CMS وCRM المحمية | اختبارات HTTP للأدوار والتعديلات التي تظهر على الصفحات العامة | لا تُغني عن اختبار الأدوار وبيانات التحرير في staging |
| الموارد المرئية والحركة | توجد اختبارات وصور محلية في scripts/performance | نتائج الأداء مرتبطة بالجهاز والعينة، ولا تضمن انعدام التقطيع على كل الأجهزة |
| إعدادات التشغيل الحية | Development وSQL Server Developer | دليل على أن الحاويات الحالية بيئة تطوير |
| مراجعة CodeRabbit | لم تُنفّذ | رفضت المراجعة التلقائية للصلاحيات تنزيل وتشغيل المثبت الخارجي مباشرة؛ النتائج هنا ليست مراجعة CodeRabbit |

نجح تشغيل 258 اختبارًا محليًا وبناء Release في 23 سبتمبر 2026. ما زال فحص SQL Server الحقيقي ومسار الشهادة خلف البروكسي يحتاجان بيئة staging فعلية.

## الموانع قبل النشر

### 1. الأسرار وحسابات الإدارة

- وُجد مفتاح API قديم في تاريخ Git المحلي داخل ملفات appsettings. ملفات الإعدادات الحالية خالية منه، والرفع يُنشأ من لقطة الملفات الحالية فوق تاريخ الريبو البعيد، دون نقل التاريخ المحلي المحتوي عليه.
- يبقى التاريخ القديم على الفرع المحلي `master`. لا ترفعه ولا تستخدم `git push --all` قبل تنقيته؛ فرع النشر النظيف هو `main`.
- يجب إلغاء المفتاح القديم وإصدار بديل من لوحة مزوّد الخدمة. حذف النص من النسخة الحالية لا يُبطل المفتاح.
- لا تُرفع `.env` أو ملفات الأسرار. اضبط بيانات الإنتاج من مخزن أسرار أو متغيرات بيئة.
- `docker-compose.yml` و`.env.example` يحتويان قيمًا افتراضية للتطوير. يجب استبدال كلمات مرور SQL والمدير بقيم فريدة؛ إنشاء المدير في بيئة Production معطل افتراضيًا ولا يُفعّل إلا مؤقتًا بمتغير `SEED_ADMIN_ALLOW_IN_PRODUCTION=true` عند التهيئة المقصودة.

### 2. HTTPS وإعدادات البيئة والبروكسي

- التشغيل الحالي `ASPNETCORE_ENVIRONMENT=Development`. يلزم نشر منفصل بإعداد `Production` واسم نطاق وشهادة TLS صالحة.
- تضيف خدمة `web` الـlabels `neurix.proxy.domain` و`neurix.proxy.port`، وقيمتهما الافتراضية `company.neurix.uk` و`18473`. متغير `NEURIX_WEB_PORT` يحدد المنفذ المنشور وقيمة label معًا، و`NEURIX_PROXY_HOSTNAME` يحدد اسم الموقع؛ متغيرا `WEB_PORT` و`NEURIX_PROXY_DOMAIN` القديمان لا يُستخدمان. ضبط `NEURIX_TRUSTED_PROXY_IP` يفعّل قراءة Forwarded Headers من عنوان البروكسي المحدد فقط قبل إعادة توجيه HTTPS والمصادقة وتحديد المعدل حسب IP. لا تثق تلقائيًا في هيدرات قادمة من الإنترنت.
- اختبر تسجيل الدخول والخروج والكوكيز الآمنة ورفع الملفات فعليًا خلف البروكسي؛ تغيير اسم البيئة وحده غير كافٍ. راجع [توثيق Microsoft لإعداد البروكسي](https://learn.microsoft.com/en-us/aspnet/core/host-and-deploy/proxy-load-balancer?view=aspnetcore-10.0).

### 3. قاعدة البيانات وعزل الشبكة

- نسخة SQL Server الحالية `Developer` مرخصة للتطوير والاختبار، وليست للإنتاج. اختر نسخة مرخصة مناسبة أو خدمة مُدارة وفق الحجم والميزانية. المصدر: [إصدارات SQL Server 2022](https://learn.microsoft.com/en-us/sql/sql-server/editions-and-components-of-sql-server-2022?view=sql-server-ver17).
- قاعدة البيانات منشورة حاليًا على منفذ المضيف `14330` بكل واجهات الشبكة. أغلق وصول الإنترنت إليها واجعل الاتصال داخليًا أو مقيدًا.
- التطبيق يستخدم حساب `sa` في Compose المحلي؛ يلزم حساب تطبيق بأقل الصلاحيات، مع حساب/خطوة منفصلة لترحيل قاعدة البيانات.
- فعّل TLS والتحقق المناسب من شهادة خادم قاعدة البيانات وفق بيئة الاستضافة.

### 4. الجاهزية والنسخ الاحتياطي

- فحص الصحة الحالي يتحقق من فتح منفذ HTTP فقط. أضف readiness يتحقق من جاهزية قواعد البيانات والترحيلات المطلوبة.
- الترحيل والتهيئة يعملان في الخلفية عند بدء التطبيق، وقد يبدأ استقبال الطلبات قبل انتهائهما. لا تعتمد على ذلك لترقية عدة نسخ إنتاجية؛ نفّذ migration مضبوطة قبل إتاحة الإصدار.
- توجد volumes للبيانات والمرفقات ومفاتيح حماية الكوكيز. وجود volume ليس نسخة احتياطية: يلزم جدول نسخ خارج المضيف، وسياسة احتفاظ، وتجربة استعادة موثقة، وخطة rollback.
- أضف رصدًا للأخطاء والتنبيهات والذاكرة والمساحة والاعتمادات الخارجية.

### 5. حماية الاستخدام العام والتحقق النهائي

- نقاط AI النصية والصوتية متاحة دون تسجيل دخول عند تفعيل المفتاح. اضبط حدود الطلبات والاتصالات والميزانية قبل فتحها للجمهور.
- CSP الحالية في وضع Report-Only؛ اختبر سياسة مناسبة ثم فعّلها بعد معالجة المخالفات اللازمة.
- اختبر على staging حقيقي: Admin وCrmStaff والمستخدم غير المسجل، محاولات الوصول المباشر، CSRF، رفع الملفات، إدخال نماذج التواصل، وفقد اتصال قاعدة البيانات.
- نفّذ اختبار حمل يناسب عدد المستخدمين المتوقع، مع قياس زمن الاستجابة والأخطاء واستهلاك الموارد، ثم اختبر على هاتف فعلي.

## أوامر إعادة التحقق

```powershell
$env:DOTNET_PROCESSOR_COUNT = '2'
dotnet restore Neurix.slnx
dotnet test Neurix.Tests/Neurix.Tests.csproj -c Release --no-restore --disable-build-servers -m:1 /p:UseSharedCompilation=false /p:BuildInParallel=false /p:ConcurrentBuild=false
dotnet list Neurix.slnx package --vulnerable --include-transitive --no-restore
npm --prefix Neurix audit --package-lock-only
docker compose up -d --build
```

أمر Compose الأخير يشغل بيئة التطوير المحلية المرفقة. لا تستخدمه كما هو باعتباره إعداد نشر إنتاجي.

## شرط قرار النشر

يُعاد تقييم القرار بعد إغلاق البنود السابقة، ونجاح staging على البنية المستهدفة، واختبار استعادة نسخة احتياطية، وتعيين مسؤول للتشغيل والتنبيهات. رفع الكود إلى GitHub لا ينشره إلى خادم إنتاج تلقائيًا.
